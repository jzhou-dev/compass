[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "compass"
version = "0.1.0"
description = "Analyze a repository and build an interactive map of its relationships."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
